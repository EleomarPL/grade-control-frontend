import { Text } from '@nextui-org/react';

const Logo = () => {
  return (
    <div style={ {fontSize: '1.5rem'} }>
      <Text h1 size="$3xl">{ '</> Qualification App' }</Text>
    </div>
  );
};

export default Logo;